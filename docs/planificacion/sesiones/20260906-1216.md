# Sesión 2026-09-06 12:16 — portado de las mejoras de los proyectos hijos

1. **Qué cambió:** comparados los seis frentes de `PruebaPlantilla` y `proyecto_2` contra la plantilla, normalizando los renombrados. Portado a `init-plantilla.yml` lo que seguía sin resolver tras la simplificación: sustitución del paquete Rust (`Cargo.toml`/`Cargo.lock`/`Dockerfile`), del usuario del runtime, de los textos de `app/src/main.rs` y del prefijo del `build` (derivado de las iniciales), el apartado de la documentación heredada a `docs/plantilla/` con su `README.md`, y un paso final que **falla el run** si queda cualquier rastro de la plantilla. Más el error real de Pages, el aviso del primer run en rojo y la unicidad global del nombre de app de Fly en `ARRANQUE.md`; la regla de `docs/plantilla/` y el «pages.yml solo se verifica en `main`» en `CLAUDE.md`; `checkout@v5` en los tres workflows. Descartado con motivo: `tools/inicializar.sh`, los `deploy.yml`/`fly.toml`/`tools/*.ps1`/`CLAUDE.md`/`README.md` de los hijos y los pasos de Fly y Drive obligatorios de su `ARRANQUE.md`.
2. **SHA:** `63c590a0fc42211aad56f8d3725985fda91c6efa` en `main`. Runs: [34032563770](https://github.com/npiobject/DesdeMovil/actions/runs/34032563770) `pages.yml` **success**, [34032563781](https://github.com/npiobject/DesdeMovil/actions/runs/34032563781) `deploy.yml` **success** (app derivada `desdemovil-npiobject`, paso *Verificar /salud* en verde), [34032563762](https://github.com/npiobject/DesdeMovil/actions/runs/34032563762) `init-plantilla.yml` **skipped**, que es su comportamiento correcto en la plantilla.
3. **URLs para probar:** https://npiobject.github.io/DesdeMovil/ (mock `DM-B3-20260905-001`, guía `DM-B3-20260906-003`) · https://desdemovil-npiobject.fly.dev/ · `/salud` → `{"ok":true,"build":"63c590a0…"}`.
4. **Drive:** copia de `portado-hijos.md` en la raíz y de este resumen en `sesiones/` de `Mi unidad/DesdeMovil` (id `1-0wWhp_-rrSgxKrr0AN34dg_Y2nAPK2J`), sin borrar nada.
5. **Qué falta:** `init-plantilla.yml` sigue sin ejecutarse de verdad en un repo generado —la barrera `is_template` lo impide aquí y se ensayó extrayendo sus `run:` sobre cuatro `GITHUB_REPOSITORY` ficticios—, así que la próxima creación desde la plantilla es la prueba real; y los `tools/*.ps1` esperan un PC con PowerShell.
