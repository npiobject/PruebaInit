# Sesión 2026-09-06 14:25 — primera ejecución real de la inicialización

1. **Qué cambió:** creado `npiobject/PruebaInit` desde la plantilla (lo creó el usuario: la integración de GitHub de la sesión no puede crear repos). La inicialización **falló**: `GITHUB_TOKEN` no puede tocar `.github/workflows/`, y el commit editaba `pages.yml` y borraba `init-plantilla.yml`, así que GitHub rechazó el push entero y se perdía todo el trabajo. Corregido en la plantilla: `pages.yml` deja de llevar marcador `PLANTILLA:`, el workflow ya no se autoborra sino que se **deshabilita por la API**, y el grep antirresiduos lo excluye. Documentado en `docs/planificacion/prueba-init.md`, con el **[SUPUESTO] 5 de `simplificacion.md` marcado como falso**: crear un repo desde plantilla sí dispara los workflows.
2. **SHA:** `7be70cb` en `DesdeMovil`/`main` (la corrección). En `PruebaInit`, run [34038895372](https://github.com/npiobject/PruebaInit/actions/runs/34038895372) **success** e inicialización aplicada en `f209929`.
3. **URLs para probar:** https://npiobject.github.io/PruebaInit/ (build `PI-B1-20260905-001`) · https://npiobject.github.io/DesdeMovil/. Verificado en el árbol de `PruebaInit`: paquete `pruebainit-backend`, usuario `pruebainit`, `GET /` → `PruebaInit backend`, **Parámetros** rellenada con Drive vacío, `docs/plantilla/` con los 7 documentos, `docs/planificacion/` limpia y `init-plantilla.yml` en `disabled_manually`.
4. **Drive:** copia de `prueba-init.md` en la raíz y de este resumen en `sesiones/` de `Mi unidad/DesdeMovil` (id `1-0wWhp_-rrSgxKrr0AN34dg_Y2nAPK2J`), sin borrar nada.
5. **Qué falta:** la corrección se probó llevando los workflows arreglados a `PruebaInit`, no sobre un repo generado de cero — **[SUPUESTO]** que se comporta igual, y confirmarlo cuesta un repo nuevo. `PruebaInit` es desechable: bórralo cuando quieras.
