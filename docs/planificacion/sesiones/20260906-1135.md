# Sesión 2026-09-06 11:35 — Simplificación de la plantilla

1. **Qué cambió**: un proyecto nuevo arranca con dos pasos manuales (crear el repo desde la plantilla y activar Pages) y nada que rellenar; Fly y Drive pasan a opcionales (`deploy.yml` termina en verde sin secreto, Drive solo si hay id en la nueva sección **Parámetros** de `CLAUDE.md`); nuevo `init-plantilla.yml` que sustituye nombre y owner y se autodestruye; `ARRANQUE.md` y `README.md` reescritos; `docs/guia.html` nueva; los dos `.ps1` comprueban `$LASTEXITCODE` tras cada git y no tocan un `repo\` ajeno.
2. **SHA**: `a5e5cd32587a851de95232cdd84070a9aa37cf0c` en `main`. `pages.yml` **success**, `deploy.yml` **success** por la vía «Fly configurado», `init-plantilla.yml` **skipped** en la plantilla, como debe ser.
3. **URLs para probar**: https://npiobject.github.io/DesdeMovil/ (mock, build `DM-B3-20260905-001`) · https://npiobject.github.io/DesdeMovil/guia.html (guía nueva, build `DM-B3-20260906-002`) · https://desdemovil-npiobject.fly.dev/salud, que devolvió `{"build":"a5e5cd32587a851de95232cdd84070a9aa37cf0c","ok":true}`.
4. **Drive**: copia de `simplificacion.md` y de este resumen subidas a la carpeta del proyecto.
5. **Qué falta**: borrar a mano la app huérfana `desdemovil-npi` en Fly (la derivación pasó a `desdemovil-npiobject`; alternativa: crear la variable de repositorio `FLY_APP=desdemovil-npi` y relanzar `deploy.yml`), y validar `init-plantilla.yml` de verdad creando un repo desde la plantilla — aquí solo se ha podido ensayar su sustitución sobre una copia local.
